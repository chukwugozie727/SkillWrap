<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Skills</title>
    <style>
        /* Modern CSS for a clean, centered layout */
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #0044cc, #007bff);
            color: #fff;
            margin: 0;
            padding: 2.5rem 1.5rem;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            box-sizing: border-box;
        }

        /* Page title */
        .page-title {
            font-size: 2.5rem;
            font-weight: bold;
            text-align: center;
            margin-bottom: 2rem;
            text-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        /* Skills grid container */
        .skills-container {
            width: 100%;
            max-width: 960px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
            padding: 0;
        }

        /* Individual skill card */
        .skill-card {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .skill-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 40px 0 rgba(0, 0, 0, 0.3);
        }

        /* Skill title */
        .skill-card h3 {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
            color: #d1e4ff;
        }

        /* Skill description */
        .skill-card p.description {
            font-size: 1rem;
            color: #c9e4ff;
            margin-bottom: 1rem;
        }
        
        /* Skill category */
        .skill-card p.category {
            font-size: 0.9rem;
            font-style: italic;
            color: #b0c9ff;
            margin-top: auto; /* Push category to the bottom */
        }

        /* Buttons container */
        .button-group {
            display: flex;
            gap: 0.5rem;
            margin-top: 1.5rem;
        }

        /* Button styling */
        .skill-card button {
            flex-grow: 1;
            padding: 0.75rem;
            border: none;
            border-radius: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s ease-in-out, background-color 0.2s ease-in-out;
            white-space: nowrap;
        }
        
        /* Edit button specific style */
        .edit-button {
            background-color: #2c3e50;
            color: #ecf0f1;
        }

        .edit-button:hover {
            background-color: #34495e;
            transform: translateY(-2px);
        }

        /* Exchange button specific style */
        .exchange-button {
            background-color: #27ae60;
            color: #fff;
        }

        .exchange-button:hover {
            background-color: #2ecc71;
            transform: translateY(-2px);
        }

        /* Empty state styling */
        .empty-state {
            text-align: center;
            padding: 3rem;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.18);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
        }

        .empty-state p {
            font-size: 1.25rem;
            color: #c9e4ff;
        }

        /* Responsive adjustments */
        @media (max-width: 600px) {
            .button-group {
                flex-direction: column;
            }

            .skill-card button {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <h1 class="page-title">My Skills</h1>
<div class="skills-container">

    <% if (skill && skill.length === 0) { %>
        <div class="empty-state">
            <p>No skills found.</p>
        </div>
    <% } else if (skill) { %>
        <% skill.forEach(item => { %>
            <div class="skill-card">
                <!-- Skill Image -->
                <div class="image-container" style="text-align:center; margin-bottom:1rem;">
                    <% if (item.skill_img) { %>
                        <img src="<%= item.skill_img %>" alt="skill image" 
                             style="max-width:100%; border-radius:12px; height:180px; object-fit:cover;">
                    <% } else { %>
                        <div class="no-image-placeholder" 
                             style="background:#eee; color:#333; padding:2rem; border-radius:12px;">
                            No image available
                        </div>
                    <% } %>
                </div>

                <!-- Skill Content -->
                <div>
                    <h3><%= item.title %></h3>
                    <p class="description"><%= item.description %></p>
                </div>
                <p class="category"><%= item.category %></p>

                <!-- Buttons -->
                <div class="button-group">
                    <button class="edit-button">Edit</button>
                    <button class="exchange-button">Request to exchange</button>
                </div>
            </div>
        <% }); %>
    <% } else { %>
        <div class="empty-state">
            <p>Skills not loaded.</p>
        </div>
    <% } %>
</div>

</body>
</html>