<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <link rel="stylesheet" href="styles/index.css">
</head>
<body>

        <div class="form-container">
        <form action="/create" method="post" class="glass-form">
               <h1>Talk About Your Skill on Skillwrap</h1>

            <% if (typeof error !== 'undefined' && error) { %>
                <p style="color: #ffcccc; text-align: center; margin-bottom: 15px;"><%= error %></p>
            <% } %>
            <div class="input-group">
                <label for="fullname">Skill Name</label>
                <input type="text" id="fullname" name="skillname" placeholder="Enter your skillname" required>
            </div>

            <div class="input-group">
                <label for="username">Describe your skill</label>
                <!-- <input type="text" id="username" name="username" placeholder="Enter your username" required> -->
                 <textarea name="skilldesc" id="desc" placeholder="describe your skill"></textarea>
            </div>

            <div class="input-group">
                <label for="phonenumber">Skill Level</label>
                <input type="text" id="phonenumber" name="skilllevel" placeholder="e.g professional, semi-pro, amatuer" required>
            </div>

            <div class="input-group">
                <label for="email">Avaliablity</label>
                <input type="text" id="email" name="avaliablity" placeholder="Times you will be avialable e.g sun, mon" required>
            </div>

            <!-- Modified: Added id to the input and the eye icon -->

            
            <button type="submit">Submit your skill</button>
            
        </form>
    </div>

</body>
</html>