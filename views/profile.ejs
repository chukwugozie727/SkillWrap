<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Profile</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      /* background: radial-gradient(circle at top left, #1a2a6c, #b21f1f, #fdbb2d); */
        background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      padding: 1.5rem;
      color: #fff;
      position: relative;
      overflow: auto; /* ✅ restored */
    }

    body::before, body::after {
      content: '';
      position: absolute;
      width: 400px;
      height: 400px;
      border-radius: 50%;
      filter: blur(120px);
      opacity: 0.6;
      z-index: -1;
      animation: float 15s ease-in-out infinite alternate;
    }
    body::before { top: -100px; left: -100px; background: #ff6ec4; }
    body::after  { bottom: -120px; right: -120px; background: #7873f5; animation-delay: 5s; }
    @keyframes float {
      from { transform: translate(0, 0); }
      to { transform: translate(60px, -40px); }
    }

    .profile {
      background: rgba(255, 255, 255, 0.12);
      backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 2rem;
      width: 100%;
      max-width: 460px;
      text-align: center;
      border: 1px solid rgba(255, 255, 255, 0.25);
      box-shadow: 0 15px 45px rgba(0, 0, 0, 0.5);
    }

    /* Image container with overlay */
    .image-container {
      position: relative;
      display: inline-block;
      margin-bottom: 1.5rem;
      cursor: pointer;
    }

    .profile img, 
    .no-image-placeholder {
      width: 160px;
      height: 160px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid rgba(255, 255, 255, 0.6);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
      transition: 0.3s;
    }

    .profile img:hover {
      transform: scale(1.05);
    }

    /* Overlay effect on hover */
    .overlay {
      position: absolute;
      inset: 0;
      border-radius: 50%;
      background: rgba(0, 0, 0, 0.5);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 0.9rem;
      font-weight: 600;
      opacity: 0;
      transition: opacity 0.3s ease-in-out;
    }
    .image-container:hover .overlay {
      opacity: 1;
    }

    /* Hide file input */
    input[type="file"] {
      display: none;
    }

    .profile h2 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
    }
    .profile p {
      font-size: 1rem;
      margin: 0.2rem 0;
      opacity: 0.9;
    }

    /* Links */
    .links-container {
      margin-top: 1.8rem;
      display: flex;
      flex-direction: column;
      gap: 0.9rem;
    }
    .links-container a {
      text-decoration: none;
      padding: 0.8rem;
      border-radius: 12px;
      font-weight: 600;
      background: rgba(255, 255, 255, 0.15);
      border: 1px solid rgba(255, 255, 255, 0.3);
      transition: 0.3s;
      color: #fff;
    }
    .links-container a:hover {
      background: linear-gradient(45deg, #ff9966, #ff5e62);
      transform: scale(1.05);
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="profile">
    <% if (user) { %>
      <form action="/post" method="POST" enctype="multipart/form-data">
        <label class="image-container">
          <% if (user.img_url) { %>
            <img src="<%= user.img_url %>" alt="Profile Picture">
          <% } else { %>
            <div class="no-image-placeholder"></div>
          <% } %>
          <div class="overlay">Add your profile pic</div>
          <input type="file" name="file" onchange="this.form.submit()" required>
        </label>
      </form>

      <h2><%= user.fullname %></h2>
      <p>@<%= user.username %></p>
      <p><%= user.email %></p>
      <p><%= user.created_at.toDateString() %></p>
      

      <div class="links-container">
        <a href="/create">➕ Create Skills</a>
        <a href="/skills">📂 View My Skills</a>
        <a href="/logout">🚪 Logout</a>
      </div>
    <% } else { %>
      <h2>Error</h2>
      <p><%= error %></p>
      <a href="/login">Go to Login</a>
    <% } %>
  </div>
</body>
</html>