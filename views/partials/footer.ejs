<!--  -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cool SkillWrap Footer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        /* Footer Base */
        .new-footer {
            background: linear-gradient(180deg, #0d1217, #0a0e12);
            color: #e0e0e0;
            padding: 60px 20px 20px;
            font-family: 'Poppins', sans-serif;
            border-top: 2px solid rgba(255, 255, 255, 0.05);
            overflow: hidden;
        }

        /* Animation keyframes */
        @keyframes fadeUp {
            0% { opacity: 0; transform: translateY(30px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        .new-footer-container {
            max-width: 1200px;
            margin: auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            animation: fadeUp 1s ease-in-out forwards;
        }

        .footer-column {
            margin-bottom: 20px;
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }
        .footer-column:hover {
            transform: translateY(-6px);
            box-shadow: 0px 6px 20px rgba(0, 123, 255, 0.15);
        }

        /* Titles */
        .footer-brand h4, .footer-nav h5, .footer-social h5, .footer-signup h5 {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 15px;
            color: #ffffff;
            position: relative;
        }
        .footer-brand h4::after, .footer-nav h5::after, .footer-social h5::after, .footer-signup h5::after {
            content: "";
            position: absolute;
            bottom: -6px;
            left: 0;
            width: 40px;
            height: 3px;
            background: linear-gradient(90deg, #007bff, #00d4ff);
            border-radius: 2px;
        }

        /* Links */
        .footer-link {
            color: #b0bec5;
            text-decoration: none;
            font-size: 0.95rem;
            line-height: 2.2;
            display: block;
            transition: color 0.3s ease, transform 0.3s ease;
        }
        .footer-link:hover {
            color: #ffffff;
            transform: translateX(5px);
        }

        /* Social Icons */
        .social-icons {
            display: flex;
            gap: 15px;
            margin-top: 10px;
        }
        .social-icons a {
            font-size: 1.5rem;
            color: #b0bec5;
            transition: transform 0.3s ease, color 0.3s ease;
        }
        .social-icons a:hover {
            color: #00d4ff;
            transform: scale(1.2);
            text-shadow: 0px 0px 10px rgba(0, 212, 255, 0.6);
        }

        /* Signup Form */
        .signup-form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        .signup-form input {
            padding: 12px;
            border: 1px solid #4a5460;
            border-radius: 8px;
            background-color: #1a2a33;
            color: #ffffff;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .signup-form input:focus {
            border-color: #00d4ff;
            box-shadow: 0 0 8px rgba(0, 212, 255, 0.3);
        }
        .signup-button {
            padding: 12px;
            background: linear-gradient(90deg, #007bff, #00d4ff);
            color: #ffffff;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
        }
        .signup-button:hover {
            transform: scale(1.05);
            background: linear-gradient(90deg, #0056b3, #0099cc);
        }

        /* Footer Bottom */
        .footer-bottom {
            text-align: center;
            margin-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            padding-top: 20px;
            font-size: 0.85rem;
            color: #78909c;
            animation: fadeUp 1.2s ease-in-out;
        }
    </style>
</head>
<body>

<footer class="new-footer">
    <div class="new-footer-container">
        
        <!-- Brand -->
        <div class="footer-column footer-brand">
            <h4>SkillWrap</h4>
            <p>Empowering you to organize and showcase your skills with style, precision, and impact.</p>
        </div>
        
        <!-- Quick Links -->
        <div class="footer-column footer-nav">
            <h5>Quick Links</h5>
            <ul>
                <li><a href="/" class="footer-link">Home</a></li>
                <li><a href="/about" class="footer-link">About Us</a></li>
                <li><a href="/dashboard" class="footer-link">Dashboard</a></li>
            </ul>
        </div>
        
        <!-- Social Media -->
        <div class="footer-column footer-social">
            <h5>Follow Us</h5>
            <div class="social-icons">
                <a href="#"><i class="fab fa-twitter"></i></a>
                <a href="#"><i class="fab fa-facebook"></i></a>
                <a href="#"><i class="fab fa-x-twitter"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
            </div>
        </div>
        
        <!-- Sign Up -->
        <div class="footer-column footer-signup">
            <h5>Stay Updated</h5>
            <p>Sign up to receive our latest news and updates.</p>
            <form action="/sign" class="signup-form">
                <input type="email" name="email" placeholder="Enter your email" required>
                <button type="submit" class="signup-button">Sign up now</button>
            </form>
        </div>
    </div>
    
    <div class="footer-bottom">
        <p>&copy; <script>document.write(new Date().getFullYear())</script> SkillWrap. All rights reserved.</p>
    </div>
</footer>

</body>
</html>
