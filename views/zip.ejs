<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles/dashboard.css">
    <!-- <link rel="stylesheet" href="styles/styles.css"> Assuming styles.css contains header styles -->
     <style>
        /*
 * General body and main container styling
 */
body {
    background-color: #f4f7f9;
}

.dashboard-main {
    padding: 20px 0;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/*
 * Welcome heading
 */
.welcome-heading {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 30px;
    color: #2c3e50;
    border-bottom: 2px solid #ecf0f1;
    padding-bottom: 10px;
}

/*
 * Dashboard grid for cards
 * Uses CSS Grid for a responsive layout
 */
.dashboard-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    margin-bottom: 40px;
}

/*
 * Styling for individual cards
 */
.card {
    background-color: #ffffff;
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
}

.card-title {
    font-size: 1.2rem;
    font-weight: 600;
    color: #7f8c8d;
    margin-bottom: 15px;
}

.card-metric {
    font-size: 3rem;
    font-weight: 700;
    color: #2c3e50;
    margin: 0;
}

/*
 * Styling for the recent activity section
 */
.section-heading {
    font-size: 1.8rem;
    font-weight: 600;
    color: #2c3e50;
    margin-bottom: 20px;
}

.recent-activity ul {
    list-style: none;
    padding: 0;
}

.recent-activity li {
    background-color: #ecf0f1;
    padding: 15px 20px;
    border-radius: 8px;
    margin-bottom: 10px;
    font-size: 1rem;
    color: #34495e;
}
     </style>
</head>
<body>

    <!-- Include the header partial -->
    <%- include("partials/header.ejs") %>

    <main class="dashboard-main">
        <div class="container">
            <h1 class="welcome-heading">Welcome back, John Doe!</h1>
            
            <div class="dashboard-grid">
                <!-- Dashboard Card 1 -->
                <div class="card">
                    <h2 class="card-title">Total Projects</h2>
                    <p class="card-metric">12</p>
                </div>
                
                <!-- Dashboard Card 2 -->
                <div class="card">
                    <h2 class="card-title">Active Tasks</h2>
                    <p class="card-metric">7</p>
                </div>

                <!-- Dashboard Card 3 -->
                <div class="card">
                    <h2 class="card-title">Completed Tasks</h2>
                    <p class="card-metric">35</p>
                </div>
            </div>

            <!-- Recent Activity Section -->
            <div class="recent-activity">
                <h2 class="section-heading">Recent Activity</h2>
                <ul>
                    <li>You started a new project: "Website Redesign"</li>
                    <li>Task "Fix login bug" was marked as complete</li>
                    <li>Uploaded 3 new files to "Marketing Assets" folder</li>
                </ul>
            </div>
        </div>
    </main>

</body>
</html>